<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cosmern POS - Panel</title>
    <link rel="stylesheet" href="/css/estilo.css">
</head>
<body>
    <%- include('parciales/encabezado', { rutaActual: '/panel', usuario: usuario }) %>

<div class="contenedor-panel">
    <h2>Bienvenido, <%- usuario %></h2>
    
    <div class="contenedor-pos">
        <div class="lista-productos">
            <h3>Productos</h3>
            <div class="cuadricula-productos" id="cuadriculaProductos">
                <!-- Productos se cargarán aquí via JavaScript -->
            </div>
        </div>
        
        <div class="contenedor-carrito">
            <h3>Venta Actual</h3>
            <div class="items-carrito" id="itemsCarrito">
                <!-- Artículos del carrito -->
            </div>
            <div class="total-carrito">
                <p>Total: <span id="totalCarrito">L 0.00</span></p>
            </div>
            <button class="boton-finalizar" id="botonFinalizar">Finalizar Venta</button>
        </div>
    </div>
</div>

<%- include('parciales/pie') %>
    <script src="/js/app.js"></script>
</body>
</html>